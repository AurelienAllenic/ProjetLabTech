# Copiez vers .env et renseignez les valeurs.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Bucket Storage (privé) pour les PDF attribués par le labo aux comptes utilisateurs.
# Créez-le dans le tableau Supabase Storage ; le backend téléverse avec la clé service role.
# SUPABASE_ASSIGNMENTS_BUCKET=assigned-documents

JWT_SECRET=

# Optionnel : origine du front (CORS)
# FRONTEND_URL=http://localhost:5173
